
  # Alva Freshman (Workspace)

  This is a code bundle for Alva Freshman (Workspace).

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
