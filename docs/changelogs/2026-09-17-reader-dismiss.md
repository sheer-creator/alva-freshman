# Persistent reader close control

Keep the thesis dialog toolbar outside its scrolling content, so Close remains visible while reading Related theses. Dismiss on a click that starts and ends outside the dialog bounds, while clicks inside and text-selection drags remain safe. Native Escape dismissal remains available. Reset the content scroll position when opening a thesis.

Scope: GUI only; also sync the isolated local lab HTML. No watchlist editing or recommendation changes. Validate scrolling, inside/outside clicks, Escape and reopening in a real browser; run syntax, snapshot tests and build before publishing.
